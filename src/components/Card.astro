---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={['bg-white rounded-2xl shadow-soft p-6 md:p-8 card-hover', className]}>
  <slot />
</div>
