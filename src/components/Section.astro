---
interface Props {
  class?: string;
  bg?: 'white' | 'gray' | 'dark' | 'subtle';
  id?: string;
}

const { class: className = '', bg = 'white', id } = Astro.props;

const bgClasses = {
  white: 'bg-white',
  gray: 'bg-gray-50/80',
  dark: 'gradient-bg text-white',
  subtle: 'bg-gradient-to-b from-blue-50/40 to-white',
};
---

<section id={id} class:list={['section-padding', bgClasses[bg], className]}>
  <div class="container-custom">
    <slot />
  </div>
</section>
